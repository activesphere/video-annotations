import React, { Component } from 'react';

import { AppHeader, FooterMenu } from "./header_and_footer";

// This 'NotesPage' is a full page currently. But I will switch to using it as a modal after
// I've designed it well enough.

export default class NotesPage extends Component {
    render() {
        return (
            <div>
                Welcome to the saved notes page.
            </div>
        );
    }
}
